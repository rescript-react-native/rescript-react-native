{
  "name": "RNTester",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "../node_modules/.bin/bsb -make-world",
    "clean-build": "../node_modules/.bin/bsb -clean-world -make-world",
    "packager:start": "node node_modules/react-native/local-cli/cli.js start",
    "watch": "../node_modules/.bin/bsb -make-world -w",
    "start": "npm run build && npm run packager:start",
    "test": "jest"
  },
  "dependencies": {
    "react": "^16.2.0",
    "react-native": "^0.56.0"
  },
  "devDependencies": {
    "babel-jest": "22.4.1",
    "babel-preset-react-native": "4.0.0"
  },
  "jest": {
    "preset": "react-native"
  }
}
